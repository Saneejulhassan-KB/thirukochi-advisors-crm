// Shared mock data for staff sidebar badge counts
// These mirror the DUMMY_TASKS and DUMMY_LEADS arrays in their respective pages
// In a real app these would come from React Query cache or a store

export const STAFF_PENDING_ASSIGNED_TASKS_COUNT = 4;
// T001 (pending, assigned), T003 (pending, assigned), T005 (pending, assigned), T010 (pending, assigned)

export const STAFF_NEW_LEADS_COUNT = 4;
// L003 (new), L005 (new), L010 (new), L012 (new)
